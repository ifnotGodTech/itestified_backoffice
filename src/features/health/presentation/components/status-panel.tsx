import { Card, CardDescription, CardTitle } from "@/core/ui/card";

export function StatusPanel() {
  return (
    <Card>
      <CardTitle>Phase 0 Baseline</CardTitle>
      <CardDescription>
        Route shell, theme system, shared primitives, and global loading/error scaffolding are in place.
      </CardDescription>
    </Card>
  );
}
